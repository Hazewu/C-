#include<iostream>
using namespace std;

int main(){
     double s=0,a[]={1,2,3,4,5,6};                        //定义变量s与数组a
     foreach(double v in a)s+=v;                         //测试foreach循环
     cout<<"数组a的各元素之和为"<<s<<endl;   

     system("PAUSE");
     return 0;
}